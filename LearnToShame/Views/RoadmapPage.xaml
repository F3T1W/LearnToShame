<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:LearnToShame.ViewModels"
             xmlns:model="clr-namespace:LearnToShame.Models"
             x:DataType="vm:RoadmapViewModel"
             x:Class="LearnToShame.Views.RoadmapPage"
             Title="Roadmap">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="15">
            <Frame BackgroundColor="{StaticResource Primary}" Padding="15">
                <VerticalStackLayout>
                    <Label Text="Current Level" TextColor="White" FontSize="14"/>
                    <Label Text="{Binding CurrentLevelName}" TextColor="White" FontSize="24" FontAttributes="Bold"/>
                    <ProgressBar Progress="{Binding ProgressValue}" ProgressColor="{StaticResource Secondary}" Margin="0,10,0,0"/>
                    <Button Text="Магазин (очки и сессии)" 
                            Command="{Binding GoToShopCommand}"
                            BackgroundColor="{StaticResource Secondary}"
                            TextColor="{StaticResource Primary}"
                            Margin="0,12,0,0"/>
                </VerticalStackLayout>
            </Frame>
            
            <Label Text="Tasks" FontSize="18" FontAttributes="Bold" Margin="0,10"/>
            
            <CollectionView ItemsSource="{Binding Tasks}">
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="model:RoadmapTask">
                        <Frame Margin="0,5" Padding="10">
                            <Frame.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type vm:RoadmapViewModel}}, Path=ToggleTaskCommand}" CommandParameter="{Binding .}"/>
                            </Frame.GestureRecognizers>
                            <Grid ColumnDefinitions="*, Auto">
                                <VerticalStackLayout Grid.Column="0">
                                    <Label Text="{Binding Title}" FontAttributes="Bold"/>
                                    <Label Text="{Binding Description}" FontSize="12" TextColor="Gray"/>
                                    <Label Text="{Binding PointsReward, StringFormat='Reward: {0} pts'}" FontSize="11" TextColor="{StaticResource Primary}"/>
                                </VerticalStackLayout>
                                <CheckBox Grid.Column="1" IsChecked="{Binding IsCompleted}" IsEnabled="False" />
                            </Grid>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
